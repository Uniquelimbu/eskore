# Server settings
PORT=5000
NODE_ENV=development

# Database connection
DB_NAME=eskore_db
DB_USER=postgres
DB_PASS=Eskore123
DB_HOST=localhost
DB_PORT=5432

# Authentication
JWT_SECRET=eskore_jwt_secret_dev_key_123

# CORS and Security
ALLOWED_ORIGINS=http://localhost:3000

# Logging
LOG_LEVEL=debug
DEBUG=express:*,auth:*
# Remove debug settings that may be causing issues