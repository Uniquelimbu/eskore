# Server settings
PORT=5000
NODE_ENV=development

# Database connection
DB_NAME=eskore_db
DB_USER=postgres
DB_PASS=POSTgres@6969
DB_HOST=localhost
DB_PORT=5432

API_DOCS='http://localhost:5000'

# Authentication
JWT_SECRET=change_this_to_a_secure_random_string

# CORS settings (Critical for frontend access)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# Logging
LOG_LEVEL=debug
DEBUG=express:*,auth:*
# Remove debug settings that may be causing issues